const subscribeToLiveRates = (user: string, endpoint: string) => {
  console.log(`Subscribing ${user} to live rates at ${endpoint}`);
};

// Example usage
subscribeToLiveRates('exampleUser', 'http://example.com');
